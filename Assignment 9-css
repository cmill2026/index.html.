/* Target the body element */
body {
  margin: 0;
}

/* Target the class site-banner */
.site-banner {
  background: black;
  text-align: center;
  padding-top: 10px;
  padding-bottom: 8px;
  padding-left: 0;
  padding-right: 0;
}

/* Set the width of the image inside site-banner */
.site-banner img {
  width: 188px;
}

/* Target the class article */
.article {
  max-width: 1200px;
  margin-left: auto;
  margin-right: auto;
  margin-top: 0;
  margin-bottom: 0;
  padding-left: 10px;
  padding-right: 10px;
}

/* Target the class story-header */
.story-header {
  font-family: "Franklin Light", sans-serif;
}

/* Target the element containing "Investigations" */
/* Adjust the selector according to the actual HTML structure */
.element-containing-investigations {
  font-family: "Franklin Bold", sans-serif;
  font-size: 14px;
  margin-top: 32px;
}

/* Target the element containing the story’s headline */
/* Adjust the selector according to the actual HTML structure */
.element-containing-headline {
  font-family: "Postoni", sans-serif;
  font-size: 46px;
  margin-bottom: 0;
  font-weight: normal;
}

/* Target the story's deck */
/* Adjust the selector according to the actual HTML structure */
.story-deck {
  height: 20px;
}

/* Target the story’s byline */
/* Adjust the selector according to the actual HTML structure */
.story-byline {
  font-size: 16px;
  margin-bottom: 2px;
}

/* Target the timestamp for publication date */
/* Adjust the selector according to the actual HTML structure */
.timestamp {
  font-size: 14px;
  margin-top: 0;
  color: #666666;
}

/* Target the class story-body */
.story-body {
  font-family: "Georgia", serif;
  font-size: 20px;
  line-height: 32px;
  max-width: 700px;
}

/* Target all images in the story */
.story img {
  width: 100%;
}

/* Target captions for images in the story */
.story figcaption {
  color: #666666;
  font-size: 14px;
  margin-top: 0;
  line-height: 18px;
  font-family: "Franklin Light", sans-serif;
}

/* Target the class story-footer */
.story-footer {
  max-width: 700px;
  border-top: 1px solid black;
}

/* Target "More for you" text in story-footer */
/* Adjust the selector according to the actual HTML structure */
.more-for-you {
  font-family: "Franklin Bold", sans-serif;
  font-size: 16px;
  font-weight: normal;
}

/* Target headlines in story-footer */
/* Adjust the selector according to the actual HTML structure */
.footer-headlines {
  font-family: "Postoni", sans-serif;
  font-size: 28px;


/* Target timestamps in story-footer */
/* Adjust the selector according to the actual HTML structure */
.footer-timestamps {
  font-family: "Franklin Light", sans-serif;
  font-size: 14px;
  padding-bottom: 10px;
  border-bottom: 1px solid #e9e9e9;

